# 순수 자바스크립트로 구현하기

## 개선

- 가상돔 구현하기
  UI 표현은 메모리에 유지되고 실제 DOM과 동기화된다. 실제 DOM은 가능한 적은 작업을 수행한다. 이 과정을 조정이라고 한다. 이전 알고리즘에서는 전체를 교체했지만 가상돔은 diff를 통해서 최소한으로 DOM을 변경한다.
  1. 애플리케이션 생성
  2. 가상 DOM 노드
  3. 변경사항
  4. Diff 알고리즘으로 기존과 다른 것을 찾아낸다.
  5. 실제 DOM으로 업데이트하되 최소한으로

## TODO

- **`applyDiff` 함수 구현이 다소 복잡하므로 추후에 여유가 있을 때 살펴보도록하고 일단은 넘어간다.**
